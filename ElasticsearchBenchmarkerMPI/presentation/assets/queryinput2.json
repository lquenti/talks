  {
    "search_queries": [
      {
        "body": {
          /* The first of 2 queries sent iteratively (random order) */
        }
      },
      {
        "body": {
          /* The second of 2 queries sent iteratively (random order) */
        }
      }
    ],
    "warmup_time_secs": 30, /* optional */
    "execution_time_secs": 180, /* optional */
    "sleep_between_requests_secs": 0.25 /* optional */
  }]
